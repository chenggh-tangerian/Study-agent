# AI 教师作业批改智能体
## 项目报告

---

| 项目信息 | 内容 |
|---------|------|
| **项目名称** | AI Agent 作业批改系统（AI 教师作业批改智能体） |
| **作者姓名** | 【填写你的姓名】 |
| **学号/班级** | 【填写学号 / 班级】 |
| **指导教师** | 【填写老师姓名】 |
| **完成日期** | 【填写日期，如 2026 年 6 月】 |
| **代码仓库** | 【填写 GitHub 链接，如 github.com/chenggh-tangerian/Study-agent】 |

---

## 摘要

本项目面向一线教师「批改耗时长、反馈难个性化、学情难追踪」三大痛点，基于 **Python + Streamlit + 阿里云通义千问大模型**，设计并实现了一套**多学科分支、结构化输出、学情可视化**的智能批改系统。

与传统「聊天式 AI 批改」不同，本项目强调 **Agent 化产品思维**：将大模型输出从「纯文本对话」升级为「可解析、可展示、可积累」的教学数据，实现了从 API 调用到教学应用的完整闭环。

**关键词：** 智能批改 · 多学科 Prompt 分支 · JSON 结构化解析 · 学情分析 · Streamlit

---

## 一、项目背景与问题提出

### 1.1 现实背景

在日常教学中，教师面临以下挑战：

| 痛点 | 具体表现 |
|------|----------|
| 批改效率低 | 一份作文/解题过程平均耗时 5–15 分钟 |
| 反馈标准不一 | 不同教师、不同次批改尺度难以统一 |
| 学情难沉淀 | 批改结果停留在纸面，无法形成数据积累 |
| 通用 AI 不好用 | 普通 ChatBot 输出冗长、格式混乱，无法直接用于教学 |

### 1.2 项目目标

设计一款**真正服务教师工作流**的轻量级 Web 应用，实现：

1. **分学科精准批改**——数学逻辑校验、文科综合、英语专项，各有独立 Prompt 策略；
2. **产品化结果展示**——隐藏 JSON / Prompt 复杂度，呈现直观的 ✅/❌、标签、评语；
3. **学情数据闭环**——历次批改自动归档，生成薄弱点统计与 AI 学情综述。

> **【截图位 1】**  
> 插入：系统首页全貌（侧边栏 + 三个 Tab 可见）  
> 建议文件名：`fig01-系统首页.png`  
> 图注：图 1  AI Agent 作业批改系统整体界面

---

## 二、技术方案与架构设计

### 2.1 技术栈选型

| 层级 | 技术选型 | 选型理由 |
|------|----------|----------|
| 前端 / 全栈 | Streamlit | 零前端基础快速构建交互界面，适合教学类 Demo |
| 大模型 | 阿里云通义千问（qwen-turbo / qwen-plus） | 国内访问稳定，百炼平台兼容 OpenAI SDK |
| API 接入 | OpenAI SDK 兼容模式 | 统一接口，便于后续切换模型 |
| 配置管理 | python-dotenv | 安全隔离 API Key，避免硬编码 |
| 数据解析 | json + 正则表达式 | 鲁棒解析大模型 JSON 输出，容错 Markdown 包裹 |

### 2.2 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    教师用户界面 (Streamlit)               │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────┐ │
│  │ 侧边栏    │  │ 作业批改台    │  │ 学情分析 / Prompt │ │
│  │ 模型/分支  │  │ 三学科动态 UI │  │ 引擎             │ │
│  └──────────┘  └──────────────┘  └──────────────────┘ │
└──────────────────────────┬──────────────────────────────┘
                           │
              ┌────────────▼────────────┐
              │   Prompt 分支引擎        │
              │  数学 JSON / 文科流式   │
              │  英语专项 / 学情综述    │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │  阿里云百炼 API         │
              │  通义千问 qwen 系列     │
              └─────────────────────────┘
```

### 2.3 核心设计亮点（★ 项目特色）

#### 亮点 1：多学科 Prompt 分支管理

不是「一个 Prompt 打天下」，而是针对学科认知特点设计**三套独立 Prompt 模板**：

| 分支 | 输出策略 | 教学价值 |
|------|----------|----------|
| 数学严谨校验 | 强制 JSON 结构化 | 像编译器一样逐步验算，精准定位出错步骤 |
| 文科综合批改 | 流式文本 + 知识点标签 | 关注结构、修辞、论点，生成鼓励性评语 |
| 英语专项批改 | 流式双语报告 | 聚焦时态、语法、词汇，附带修改示范 |

> **【截图位 2】**  
> 插入：侧边栏三个学科分支 + Tab 3 提示词引擎界面  
> 图注：图 2  多学科 Prompt 分支与可编辑提示词引擎

#### 亮点 2：Agent 化结构化输出（数学模式）

数学模式要求大模型输出标准 JSON，后端通过 `parse_json_response()` 函数：
- 正则去除 ` ```json ` 包裹；
- 提取 `{...}` 主体；
- `json.loads` 解析后映射为 Streamlit 组件。

**教师看到的是：**

```
✅ 该题解答完全正确！          ← st.success
❌ 发现逻辑错误或计算失误      ← st.error
出错步骤（蓝色卡片）           ← st.info
错误原因（黄色卡片）           ← st.warning
正确解法参考（代码块）         ← st.code
```

而非原始 JSON 文本——体现「从接口到产品」的跨越。

> **【截图位 3】**  
> 插入：数学批改 ❌ 错误结果（含 info / warning 卡片）  
> 图注：图 3  数学模式结构化校验结果展示

> **【截图位 4】**  
> 插入：数学批改 ✅ 正确结果  
> 图注：图 4  数学模式正确解答反馈

#### 亮点 3：流式输出提升体验（文科 / 英语）

文科与英语模式采用 `stream=True` + `st.write_stream`，实现打字机效果，降低等待焦虑，更贴近真实「老师逐句点评」的体验。

> **【截图位 5】**  
> 插入：英语批改流式输出进行中或完成后的界面  
> 图注：图 5  英语专项批改流式输出效果

#### 亮点 4：学情分析仪表盘（★ 差异化功能）

每次批改自动写入 `session_state` 历史记录，学情 Tab 提供：

- 📊 **指标卡片**：批改次数、平均分、数学正确率、薄弱标签数；
- 📈 **高频薄弱知识点**柱状图；
- 📚 **学科分布**统计；
- 📋 **批改历史明细**表格；
- 🧠 **AI 学情综述**：一键生成含「总览 / 薄弱点 / 建议 / 练习方向」的分析报告。

**这是普通 AI 聊天工具不具备的「数据沉淀 → 教学决策」能力。**

> **【截图位 6】**  
> 插入：学情分析 Tab — 指标卡片 + 薄弱点柱状图  
> 图注：图 6  学情分析仪表盘

> **【截图位 7】**  
> 插入：AI 学情综述生成结果（流式输出）  
> 图注：图 7  AI 自动生成学情分析报告

#### 亮点 5：提示词引擎（可定制 / 可复现）

Tab 3 允许教师或开发者**直接编辑 System Prompt 模板**，修改后即时生效，支持恢复默认。体现：

- Prompt Engineering 的工程化落地；
- 批改标准可定制、可复现、可迭代。

> **【截图位 8】**  
> 插入：提示词引擎 Tab，展示 Prompt 模板编辑区  
> 图注：图 8  可编辑的 Prompt 提示词引擎

---

## 三、功能演示与测试验证

### 3.1 测试方案

项目配套 `test_cases.md`，覆盖：

| 学科 | 用例数 | 测试重点 |
|------|--------|----------|
| 数学 | 3 | 计算错误、完全正确、逻辑跳跃 |
| 文科 | 2 | 议论文结构、修辞语病 |
| 英语 | 3 | 过去时态、主谓一致、书信格式 |
| 学情 | 7 步流程 | 记录积累 → 图表 → AI 综述 |

### 3.2 典型测试案例

#### 案例 A：数学移项符号错误

**输入：**
- 题目：解方程 2x + 5 = 13
- 学生解答：2x = 13 + 5 → x = 9（移项符号错误）

**系统输出：**
- ❌ 发现逻辑错误
- 出错步骤：移项环节
- 错误原因：移项未变号
- 标签：#移项法则 #一元一次方程

> **【截图位 9】**  
> 插入：上述数学错题的批改结果  
> 图注：图 9  数学错题校验测试案例

#### 案例 B：英语过去时态错误

**输入：**
- 侧重点：一般过去时
- 学生作业：`Last summer, I go to Beijing...`（多处时态错误）

**系统输出：**
- 标签：#PastTense #WordChoice
- 逐句错误指正 + 修改示范 + 鼓励评语

> **【截图位 10】**  
> 插入：英语批改结果  
> 图注：图 10  英语时态批改测试案例

#### 案例 C：学情分析闭环

连续批改 4 次（数学 2 + 文科 1 + 英语 1）后：

| 指标 | 结果 |
|------|------|
| 批改次数 | 4 |
| 数学正确率 | 50% |
| 高频标签 | #移项法则、#PastTense 等 |

> **【截图位 11】**  
> 插入：学情分析历史明细表格 + 学科分布图  
> 图注：图 11  多轮批改后的学情数据积累

---

## 四、创新点与项目价值

### 4.1 创新点总结

| 维度 | 普通 AI 聊天工具 | 本项目 |
|------|------------------|--------|
| 学科适配 | 通用对话 | 三套分支 Prompt，策略各异 |
| 输出形式 | 纯文本 | JSON 结构化 + 流式 + UI 组件映射 |
| 数据沉淀 | 无 | 自动记录，学情仪表盘 + AI 综述 |
| 可定制性 | 不可见 | Prompt 引擎可视化编辑 |
| 目标用户 | 通用 | 一线教师工作流 |

### 4.2 教学应用价值

1. **减负增效**：单次批改从 10 分钟缩短至 1–2 分钟（教师复核）；
2. **标准统一**：Prompt 模板固化批改尺度，减少主观偏差；
3. **精准辅导**：知识点标签 + 学情综述，支撑分层教学与个性化辅导；
4. **家校沟通**：评语模块可直接复制发给家长（文科 / 英语模式）。

### 4.3 技术能力体现

- ✅ 大模型 API 接入与流式处理
- ✅ Prompt Engineering 与分支管理
- ✅ JSON 鲁棒解析与异常容错
- ✅ Session State 状态管理
- ✅ 数据可视化（Streamlit 图表组件）
- ✅ 完整测试用例与文档

---

## 五、项目文件结构

```
project1/
├── app.py              # 主程序（~700 行，含完整注释）
├── requirements.txt    # 依赖清单
├── prd.md              # 产品需求文档
├── test_cases.md       # 测试用例手册
├── .env                # API Key 配置（不提交 Git）
├── .gitignore          # 忽略密钥与虚拟环境
└── 项目报告模板.md      # 本报告
```

> **【截图位 12】**  
> 插入：VS Code / Cursor 项目目录结构 或 GitHub 仓库页面  
> 图注：图 12  项目代码结构与版本管理

---

## 六、不足与展望

### 6.1 当前局限

| 局限 | 说明 |
|------|------|
| 数据持久化 | 学情记录存于 session_state，刷新页面后丢失 |
| 并发能力 | 单机 Streamlit，不适合大规模校内部署 |
| 图片作业 | 暂不支持拍照上传 OCR 识别 |
| 模型依赖 | 批改质量受大模型能力与网络影响 |

### 6.2 后续改进方向

1. **数据库持久化**：接入 SQLite / PostgreSQL，支持班级维度学情追踪；
2. **批量批改**：支持上传 Excel / CSV 批量处理；
3. **OCR 接入**：支持手写体 / 拍照作业识别；
4. **班级管理**：教师账号、学生档案、学期报告导出；
5. **RAG 知识库**：接入教材/考纲，提升批改依据的可溯源性。

---

## 七、个人收获与总结

【填写 150–300 字，建议从以下角度展开】

- 对大模型应用从「调 API」到「做产品」的认知转变；
- Prompt Engineering 中分支设计、JSON 约束的实践体会；
- 以教师用户为中心做 UI / UX 设计的思考；
- 遇到的技术难点（如 JSON 解析容错、session_state 管理）及解决过程；
- 对 AI + 教育融合前景的看法。

---

## 附录

### 附录 A：运行环境

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 .env
DASHSCOPE_API_KEY=你的密钥

# 3. 启动
streamlit run app.py
```

### 附录 B：截图清单（提交前核对）

| 编号 | 截图内容 | 状态 |
|------|----------|------|
| 图 1 | 系统首页全貌 | ☐ |
| 图 2 | 学科分支 + 提示词引擎 | ☐ |
| 图 3 | 数学 ❌ 错误结果 | ☐ |
| 图 4 | 数学 ✅ 正确结果 | ☐ |
| 图 5 | 英语流式批改 | ☐ |
| 图 6 | 学情分析仪表盘 | ☐ |
| 图 7 | AI 学情综述 | ☐ |
| 图 8 | Prompt 引擎编辑 | ☐ |
| 图 9 | 数学测试案例 | ☐ |
| 图 10 | 英语测试案例 | ☐ |
| 图 11 | 学情历史明细 | ☐ |
| 图 12 | 代码 / 仓库结构 | ☐ |

### 附录 C：参考文献

1. 阿里云百炼平台文档：https://help.aliyun.com/zh/model-studio/
2. Streamlit 官方文档：https://docs.streamlit.io/
3. OpenAI SDK 兼容模式说明（DashScope）

---

**报告撰写人：** 【你的姓名】  
**日期：** 【日期】

---

> 💡 **使用提示**
> 1. 将本模板复制到 Word / WPS，替换所有【】占位符；
> 2. 按「截图位」编号依次截屏插入，建议统一宽度（如 14cm）；
> 3. 每张图下方加图注（模板中已写好，可直接用）；
> 4. 第七章「个人收获」务必手写，是老师最看重的部分；
> 5. 提交前对照附录 B 截图清单逐项打勾。
