
# 📄 产品需求文档 (PRD)：AI教师作业批改助手

## 一、 产品概述
**产品名称**：AI教师作业批改助手 (AI Homework Grader)
**产品定位**：一款为一线教师减负的轻量级智能网页应用。通过接入阿里云通义千问大模型，实现多学科作业的自动批改。
**核心亮点**：支持“动态评分标准自定义”、自动提取“知识点错题标签”、以及“学生/家长双轨制评语生成”。

## 二、 技术栈与环境约束
*   **前端/全栈框架**：Python + `Streamlit` (要求界面简洁美观，使用 st.sidebar 等组件)。
*   **大模型接口**：阿里云百炼平台 API (使用 `dashscope` 库或 OpenAI SDK 兼容模式调用)。
*   **鉴权方式**：从 `.env` 文件或系统的环境变量中读取 `DASHSCOPE_API_KEY`。
*   **输出方式**：必须使用**流式输出 (Streaming)**，提升用户体验（打字机效果）。

## 三、 页面布局与交互设计 (UI/UX)

应用界面分为 **侧边栏 (Sidebar)** 和 **主区域 (Main Area)** 两部分。

### 3.1 侧边栏 (Sidebar) - 配置区
1.  **应用标题**：显示应用 Logo/名称。
2.  **大模型选择器 (st.selectbox)**：
    *   选项 1："通义千问-Turbo (极速版)" -> 对应模型名 `qwen-turbo`
    *   选项 2："通义千问-Plus (深度版)" -> 对应模型名 `qwen-plus`
    *   选项 3："通义千问-Max (专家版)" -> 对应模型名 `qwen-max`
3.  **科目选择器 (st.selectbox)**：
    *   选项："语文作文", "数学解题", "英语作文", "编程代码"

### 3.2 主区域 (Main Area) - 操作与结果区
1.  **大标题**：`👨‍🏫 AI 教师作业批改助手`
2.  **功能 1：动态评分标准自定义 (st.text_input)**
    *   **标签**：`🎯 本次批改侧重点（选填，例如：重点检查过去时态、重点检查修辞手法）`
    *   **作用**：用户填写的这句话，将被动态插入到 System Prompt 中，指导大模型的注意力权重。
3.  **功能 2：学生作业输入区 (st.text_area)**
    *   **标签**：`📝 请在此粘贴学生的作业内容`
    *   **高度**：建议设置 height=300，方便粘贴长文本。
4.  **操作按钮 (st.button)**：
    *   `🚀 开始智能批改`
    *   点击后触发后端调用逻辑，并在下方展示结果。
5.  **结果展示区**：
    *   使用 `st.write_stream` 或相应的流式组件，逐字打印大模型返回的结果。

## 四、 核心功能逻辑与 Prompt 架构设计 (⚠️ 关键)

为了实现“知识点打标签”等高级功能，后端在拼接发送给大模型的 Prompt 时，**必须严格按照以下结构组装 `System Prompt`**。

### 4.1 动态 Prompt 组装逻辑
当用户点击“开始批改”后，系统按如下逻辑生成指令：

*   **变量 `{course}`**：读取侧边栏的科目选择。
*   **变量 `{rubric}`**：读取主区域的“批改侧重点”。如果用户为空，则默认为“按该学科的常规标准进行全面批改”。
*   **变量 `{homework}`**：读取学生作业输入框的内容。

### 4.2 核心 Prompt 模板 (请写进代码中)
```text
你现在是一位资深的【{course}】学科教师。你需要帮我批改一份学生的作业。

【本次批改侧重点】：
{rubric}

【输出格式要求】：
请严格按照以下5个模块输出你的批改结果，并使用相应的 Emoji 图标作为模块标题：

1. 🏷️ 薄弱知识点标签
(请根据作业中的错误，提炼出1-3个知识点标签，使用 #标签名 的格式，例如 #被动语态错误 #逻辑不严密)

2. 📊 综合评分
(给出百分制分数，或 A/B/C/D 等级，并给出一句话总评)

3. ❌ 错误指正与批改详情
(详细指出作业中的具体错误位置，解释为什么错，并给出正确的修改建议或参考答案)

4. 👧 给学生的评语
(用亲切、鼓励的口吻写一段话，肯定优点，委婉指出不足，直接对学生说)

5. 👨‍👩‍👦 给家长的反馈
(用客观、专业的口吻写一段话，概括孩子的作业情况和薄弱点，适合老师直接复制发给家长微信)

【学生作业内容如下】：
{homework}
```

## 五、 异常处理与边界条件
1.  **空输入拦截**：如果未输入作业内容就点击批改，需使用 `st.warning` 提示“请输入作业内容”。
2.  **API 错误捕获**：如果阿里云 API 调用失败（如 key 无效、网络超时），需使用 `st.error` 捕获异常并显示错误信息，防止程序崩溃。

---

## 🛠️ 附：给 Cursor 的执行指令

*(当你把上面的 PRD 发给 Cursor 后，紧接着发下面这段话)*

> **@Cursor，请阅读上述 PRD 文档，并执行以下操作：**
> 1. 使用 Python 和 Streamlit 生成 `app.py`。
> 2. 使用官方推荐的 `openai` 库（因为阿里云百炼已全面兼容 OpenAI SDK），其 Base URL 配置为 `https://dashscope.aliyuncs.com/compatible-mode/v1`。
> 3. 代码中需要处理好状态管理（session_state）和流式输出显示。
> 4. 请确保代码结构清晰，包含完整的注释，并在代码开头提示我需要安装哪些依赖（如 `pip install streamlit openai`）。
> 5. 直接给我完整的 `app.py` 代码。